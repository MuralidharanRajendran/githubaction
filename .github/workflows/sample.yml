name: shell commands
on : [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name : echo a string
        run : echo "Hellp World"
      - name : Multiple line script
        run: |
          node -v
          npm -v